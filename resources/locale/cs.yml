fof-oauth:
  admin:
    settings:
      providers:
        seznam:
          description: "Zaregistrujte OAuth aplikaci na {link} a nastavte návratovou URL na callback URL vašeho fóra."

          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret

  forum:
    log_in:
      with_seznam_button: "Přihlásit přes {provider}"

    providers:
      seznam: '=> fof-oauth.lib.providers.seznam'

  lib:
    providers:
      seznam: Seznam.cz
