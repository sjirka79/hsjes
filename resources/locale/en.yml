fof-oauth:
  admin:
    settings:
      providers:
        seznam:
          description: "Register an OAuth application at {link} and set the redirect URI to your forum's callback URL."

          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret

  forum:
    log_in:
      with_seznam_button: '=> fof-oauth.forum.log_in.with_button'

    providers:
      seznam: '=> fof-oauth.lib.providers.seznam'

  lib:
    providers:
      seznam: Seznam.cz
